# Replit
# Khởi tạo Git
git init

# Thêm tất cả file vào hàng chờ
git add .

# Ghi chú cho lần đẩy code này
git commit -m "First commit"

# Dán cái link GitHub bạn vừa copy vào sau chữ remote add origin
git remote add origin <DÁN_LINK_CỦA_BẠN_VÀO_ĐÂY>

# Đẩy code lên (Nó sẽ hỏi Username và Password/Token của bạn)
git push -u origin main
